<section class="navigation">
	<nav class="navbar navbar-expand-lg customNav">
		<div class="navContainer container">
			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav mx-auto mb-2 mb-lg-0">
					<% Region.active.each do |region| %>
						<li class="nav-item dropdown">
							<a class="nav-link dropdown-toggle" href="#" id="navbarDropdown#{region.id}" role="button" data-bs-hover="dropdown" aria-expanded="false">
								<%= region.region_name %>
							</a>
							<ul class="dropdown-menu" aria-labelledby="navbarDropdown#{region.id}">
								<div class="container">
									<div class="row">
										<div class="destination"> <span>Countries</span> </div>
										<div class="col-md-6 borderRight">
											<p class="blue bold mb-0 py-2 small">Countries</p>
											<% region.countries.active.each do |country| %>
												<li class="innerLink"><span class="icon">> </span>
													<a href="/all_tours?country=<%= country.name %> ">
														<%= country.name %>
													</a>
												</li>
												<% end %>
										</div>
							
									</div>
								</div>
							</ul>
						</li>
						<% end %>
				</ul>
			</div>
		</div>
	</nav>
</section>
<section class="navigation1 d-lg-none">
	<nav class="navbar navbar-expand-lg customNav1 ">
		<div class="container-fluid">
			<a class="navbar-brand" href="/">
      <%= image_tag "logo.png", class:"img-fluid  mainLogoMobile", width: "100px" %>
      </a>
			<button class="navbar-toggler togglerBtn" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"> <i class="fa-solid fa-bars togglerIcon"></i> </button>
			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<div class="accordion" id="mobile-navbar">

					<% Region.active.each do |region| %>

		
					<div class="accordion-item p-0">
            <h2 class="accordion-header" id="headingOne">
              <button class="accordion-button collapsed px-2" type="button" data-bs-toggle="collapse"
                data-bs-target="#c<%= region.id %>" aria-expanded="true" aria-controls="c1">
                	<%= region.region_name %>
              </button>
            </h2>
						<div id="c<%= region.id %>" class="accordion-collapse collapse" aria-labelledby="headingOne" data-bs-parent="#mobile-navbar">
							<div class="accordion-body">
								<ul class="navbar-nav me-auto mb-2 mb-lg-0">
									<% region.countries.active.each do |country| %>

									<li class="nav-item">	
                    <a href="/all_tours?country=<%= country.name %> ">
												<%= country.name %>
										</a>
                    </li>

                  <% end %>
								</ul>
							</div>
						</div>
					</div>

          <% end %>
          
          

				</div>
			</div>
			</div>
	</nav>
</section>